---
scope: Datadog 风格 Trace Timeline 独立 React 组件库
purpose: build substrate（供 Epic/故事与实现 agent 遵循的架构骨架）
altitude: initiative→features（整库系统级）
updated: 2026-06-25T16:09
---

- (decision) D1 接缝：共享引擎(虚拟滚动/树/数据/时间映射/状态) + Datadog 表现层重写(行/条/标签列)。单皮肤、抽象最少。Prevents: 表现层差异污染引擎、未来引擎与上游难对照
- (decision) D2 范式：引擎核心(VirtualizedTraceView/ListView/Positions)照搬 class+memoizeOne(低风险/便于对照上游)；新写的 Datadog 表现层与容器用 FC+hooks。Prevents: 重写虚拟滚动引入生命周期行为偏差
- (decision) D3 依赖方向：反转。core 暴露 rowRenderer(RenderableRow)契约、零 Datadog import；api/<TraceTimeline> 注入 Datadog 行渲染器。单一注入点非全插槽。Binds: core 纯引擎+Apache 来源纯净
- (decision) D4 七层模块：core(引擎)/presentation(Datadog皮肤)/state(hooks容器)/model(数据)/theme(DRUIDS令牌)/ui(原语)/api(对外)。依赖：api→{core,presentation,state}；presentation→{theme,ui,model}；core→model；皆不反向
- (decision) D5 状态：组合 Grafana 现成5 hook(children/detail/viewRange/search/hover)+列宽，外套 useTraceTimelineState 做受控回退。受控粒度：默认全非受控；focusedSpanId+搜索query 可受控；全量受控逃生舱；列宽/hover 仅非受控。Datadog 加 colorBy/errorsOnly(errorsOnly 复用搜索过滤)
- (decision) D6 着色：抽象 colorAccessor(span)=>colorKey + Datadog 分类色板；内置 colorBy='service'(v1唯一)；下拉其余维度占位；允许自定义 colorAccessor
- (decision) D7 数据：对外公共输入=派生 Trace；导出 transformTraceData(TraceResponse)=>Trace(v1 Jaeger JSON,微秒)；model/adapters/ 预留 fromOTLP() v2。契约=派生模型,适配器=边界
- (decision) D8 主题：createTheme({colorMode,override})=>Theme；令牌沿用 GrafanaTheme2 子集形状(值填DRUIDS)+新增 theme.trace 命名空间(条高/圆角2px顶/gap/分类色板/状态色/缩进线/选中行底);override 深合并回退内置;useStyles2 按 theme memo
- (decision) D9 构建：tsup 出 ESM+CJS+d.ts；exports map；sideEffects:false；react/react-dom peer(^18||^19)；vitest+Testing Library 单测；Storybook+README
- (decision) D10 颜色归属：core 只传数据(RenderableRow 含 ancestorSpanIds 等,无颜色)；presentation 用 colorAccessor+theme.trace 调色板着色与画服务色缩进竖线。Prevents: core 引入调色板破坏纯引擎(D3)
- (constraint) 不变量：所有状态更新必须不可变(新 Set/Map/数组)——引擎依赖 memoizeOne 引用相等做 generateRowStates/viewedBounds 缓存,可变更新会导致漏更新或缓存失效
- (event) 对抗评审 NEEDS-WORK：7洞——行高归属未定/颜色接缝RPC合并歧义/viewBounds单位+ancestorSpanIds顺序歧义/theme.trace未类型化/colorGenerator来源自相矛盾(FR-3 vs AD-9)/受控双真相源/沉默维度(行key+空态+a11y)
- (change) 对抗评审7洞全闭：AD-2 精确化 RenderableRow(viewBounds投影值+clipping/ancestorSpanIds根→父length===depth/isError与descendantHasError分离/rpc.process数据着色)；AD-5 加受控优先级互斥；AD-6 加RPC着色+colorGenerator自实现非照搬；AD-7 theme.trace 改导出TS接口TraceThemeTokens+status单一住所；新增 AD-12 行高归core固定常量/AD-13 行key归core；conventions 补空态/a11y焦点；Deferred 补完整a11y
- (event) spine finalized
- (override) 范围转向：放弃后端无关硬目标，改对接 DataFox。实测 /api/v3/spans/search 返回 Grafana DataFrame 列式(OTLP字段:trace_id/span_id/parent_span_id/ts(ms)/dur(ns)/kind/status/exception/attrs_raw/events)；trace_id 过滤可拉单 trace；孤儿父按 root
- (decision) AD-8 改 DataFox 适配器(自写列式解析+复用Apache transform,ms/ns→µs)；新增 AD-14 可选 fetchTrace 助手层 data/datafox，<TraceTimeline> 仍 props 驱动。Deferred 去 OTLP/Jaeger 适配,加 trace-list 范围外
- (event) spine updated (DataFox 转向)，lint 0 findings
- (override) 范围转向：内部工具不对外发布→AGPL 不触发。AD-9 改为无义务、Grafana 代码原样移植(含CriticalPath/filter-spans/useSearch)、保留版权头、不做audit/NOTICE；AD-10 改 ESM-only 去CJS/SemVer/体积预算/Storybook；AD-6 colorGenerator 改直接移植
